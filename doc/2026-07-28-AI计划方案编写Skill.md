# AI 计划方案编写 Skill

> 日期：2026-07-28  
> 状态：已实现  
> 供其它智能体与后续会话对照；落实 CLAUDE.md「计划入 doc」。

## 目标

把 `doc/` 中已形成的计划写法提炼为项目级 Cursor Skill，使「给其它智能体可执行」的方案结构、命名、状态与验收约定显式化、可自动触发。

## 实现要点

### `.cursor/skills/ai-plan-writing/SKILL.md`

- 硬性规则：先问后做、计划落 `doc/`、机制同步 `design/`、中文、不批量改旧文件名
- 工作流 + 三类精简模板（Bug / 功能小重构 / 大型机制）
- 文首元信息与状态机、可执行性自检、反例

### `.cursor/skills/ai-plan-writing/templates.md`

- 三类可复制完整 Markdown 骨架

### `.cursor/skills/ai-plan-writing/examples.md`

- 链到 `doc/` 优质样例 + 好/坏对照 + 选用提示

### `CLAUDE.md`

- AI 限制增加一行指针到本 Skill

## 验收

1. [x] 存在 `.cursor/skills/ai-plan-writing/SKILL.md`，含 description 触发词，未设 `disable-model-invocation`
2. [x] 存在 `templates.md`、`examples.md`，且 SKILL 内单向链接可到达
3. [x] `CLAUDE.md` 指向该 Skill
4. [x] 本执行文档落在 `doc/2026-07-28-AI计划方案编写Skill.md`

## 明确不做

- 不批量改写历史 `doc/` 文件名
- 不引入脚本校验器
- 不做成个人 skill（`~/.cursor/skills/`）
