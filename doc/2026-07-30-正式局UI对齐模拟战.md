# 正式局 UI 对齐模拟战 — 实现计划

> 日期：2026-07-30  
> 状态：已实现  
> 依据：计划「正式局对齐模拟战 UI」；视觉/交互以 `sim-battle.ts` + `#sb-*` 为准。  
> **不使用** YOTOO / Java 微服务规范。

## 目标

保留正式局玩法（事件、购售、存档、对战池、方案 B 分栏），构筑与战斗控件对齐模拟战。

## 差异（必须保留）

- 仓库/商人带金币；商人面板 = 出售落点
- 仓库顶层允许单独词条
- 无敌方编成编辑；战斗敌方只读

## 任务

1. ~~抽取 `ui/build/`：`entityCard`、`poolList`、`simTooltip`；模拟战改为引用~~
2. ~~正式探险壳：商人池 + BD 卡片 + 仓库（允单独词条）+ pointerDrag + 购售~~（`officialExplore.ts` + `panels.ts`）
3. ~~正式战斗壳：sb 卡片树 + `mountBattleLog` + `bindPlaybackControls`~~（`officialCombat.ts` + `panels.ts`）
4. ~~扩展 `pointerDrag` 以支持正式局 zone（warehouse / shop 出售）~~
5. ~~更新 `DESIGN-正式局.md` / `01-游戏概述.md`~~

## 实现要点（战斗壳）

| 路径 | 改动 |
|------|------|
| `project/client/src/ui/officialCombat.ts` | 新建：两侧 `sb-battle-side` 卡片树、中栏摘要+`#fg-battle-log`、`patchOfficialBattleValues`、折叠/tooltip |
| `project/client/src/ui/panels.ts` | 战斗渲染改走 officialCombat；rAF patch；开战先跑 `runCombatWithSides` 再渲染；结束态缓存 final units / enemy slots；HUD `bindPlaybackControls` |
| `project/client/styles/global.css` | 战斗壳下 `.sb-battle-side` / `#fg-battle-log` 可滚动 |
| `design/DESIGN-正式局.md` / `design/01-游戏概述.md` | 战斗壳注明共用 sb 组件 |

## 验收

- [x] 商人筛选/列表/悬浮 ≈ 模拟战物品池
- [x] BD/仓库卡片拖拽；仓库可放单独词条
- [x] 战斗双方卡片 + Solid 日志 + 倍速
- [x] `cd project/client; npx tsc --noEmit` 通过
- [ ] 模拟战回归不回退（人工冒烟）
