# 探险壳 BD / 仓库视觉对齐

## 目标

放大正式局 BD、仓库可拖落点；去掉「出场面板」套娃与「拖入启动端到第一层」教学文案；区头与空态对齐 `08-正式局设计系统` 工作台语言。

## 改动

| 文件 | 内容 |
|------|------|
| `project/client/src/ui/officialExplore.ts` | 去 `panel`；`.fg-zone-head`；空态 `.fg-drop-empty` |
| `project/client/styles/global.css` | `#deploy-area` / `#warehouse-area` / `#fg-*` 高度链；区头与虚线空态 |
| `design/08-正式局设计系统.md` §5.1 | 区头单层、空态文案约定 |

## 文案

- BD 空：`拖入实体`
- 仓库空：`拖入实体或词条`

## 验收

- 空 BD / 空仓库虚线区占满栏高
- 无「出场面板」「拖入启动端到第一层」
- 拖拽买卖/卸下不变；模拟战不受影响
