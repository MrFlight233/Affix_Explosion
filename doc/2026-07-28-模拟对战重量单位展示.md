# 模拟对战重量单位自适应展示

> 日期：2026-07-28  
> 状态：已实现  
> 范围：仅 sim-battle UI；存储与战斗仍用克

## 目标

重量 / 负重上限 / 负重加成展示：`<1000g` 用 `g`，`≥1000g` 用 `kg`（如 `80kg`、`500g`）。

## 实现要点

- [`project/client/src/ui/sim-battle.ts`](project/client/src/ui/sim-battle.ts)：`formatWeightG` / `formatWeightBonusG`；tip 与卡片全部走格式化
- [`design/02-名词解释.md`](design/02-名词解释.md)：补充 UI 展示约定

明确不做：panels / tooltip / Admin；不改 DB。

## 验收

- [x] 80000→`80kg`；500→`500g`；1000→`1kg`；1500→`1.5kg`
- [x] tip/卡片重量相关字段统一格式化
- [x] 超重逻辑仍按克
