## 项目介绍

《Affix Explosion》是一款界面简洁的纯文字肉鸽爬塔游戏。

### 语言

主要使用：中文

## AI限制

- 主要使用语言：中文。
- 先询问，后执行。
- 当进行修改时，要同时修改design文件夹下的相关设计文档，这个文件下的文档描述了游戏的机制与解释。文档地图见 `design/README.md`（`01`～`06` 玩法；`07`～`08` UI 设计系统）。**design 正文不链接 doc**（doc 为计划/日志；design 为现行设计）。
- **事件文档同步**：凡增删改探险事件（`exploreEvents` 定义、出现规则 must/can、货架/价格/交互、事件文案），必须同步更新 `design/06-事件介绍.md`（逐事件专节 + 文首一览表）；`design/02-名词解释.md` 事件摘要表若有字段变化一并改。不得只改代码不改 06。
- **效果文档同步**：凡增删改效果库配方规则、主动/被动通道引用、结算落点/合并规则、可引用勾选语义，必须同步更新 `design/09-效果介绍.md`；`design/02`/`03`/`04`/`05` 相关摘要一并改。不得只改代码不改 09。
- design 文档地图含 `09-效果介绍.md`（效果库与通道）。
- 当做计划时，每次要记录成文档，放入doc文件夹下，修改计划时也要修改文档。这个文档是用来给其它智能体对话执行使用的。
- 计划方案的结构、命名、验收与模板详见项目 Skill：`.cursor/skills/ai-plan-writing/`（触发词：计划、方案、写入 doc、给其它智能体执行）。
- **计划文档必须包含四段**：现状（当前行为与问题）→ 期望（目标行为）→ 原因（为什么这样设计）→ 实现（具体改动点与落点文件）。不允许只描述实现而省略设计意图。
- **一个方案可能包含多个修改点**：每个修改点彼此独立（可能有关联但各自有完整的逻辑），每个修改点必须有自己独立的四段（现状/期望/原因/实现）。不允许把多个修改点的设计意图混在一起描述。
- 不要使用MCP中的开发规范。
- 游戏模板数据以 `project/server/data/seed/` 下 JSON 种子纳入 Git；运行时 `*.db` / `*.db-shm` / `*.db-wal` 不入库。
- **提交种子文件前必须主动导出**：凡本次提交会包含或更新 `project/server/data/seed/`，在 `git add` / `commit` 之前必须先执行 `cd project/server && npm run seed:publish`，用当前本地 DB 覆盖写出种子 JSON，再暂存种子文件。不得假设用户已点击 Admin「发布种子」按钮；若本地库为空或导出失败，应先说明并停止提交种子，而不是提交过期/空种子。

